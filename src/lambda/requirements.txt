# AWS SDK
boto3>=1.28.0

# AWS IoT SDK (für Sensor-Simulator)
AWSIoTPythonSDK>=1.5.0

# Environment Variables (für lokale Entwicklung)
python-dotenv>=1.0.0

# HTTP Requests (für API-Simulator)
requests>=2.31.0

# Optional: Für bessere Tests
pytest>=7.0.0
